module	rom_top
(
	input clk,
	input rst_n,
	output [7:0] q
);

wire addr;

rom_ctrl	rom_ctrl_inst
(
	.clk(clk),
	.rst_n(rst_n),
	.addr(addr)
);

my_rom my_rom_inst
(
	.address(addr),
	.clock(clk),
	.q(q)
);


endmodule